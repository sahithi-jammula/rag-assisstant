# Used for local runs and Docker. App Runner / some hosts set PORT; entrypoint passes --server.port.

[server]
headless = true
address = "0.0.0.0"
port = 8501
enableXsrfProtection = true
fileWatcherType = "none"

[browser]
gatherUsageStats = false
