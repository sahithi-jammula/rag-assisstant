# Streamlit server defaults (local or when binding a non-default host).

[server]
headless = true
address = "0.0.0.0"
port = 8501
enableXsrfProtection = true
fileWatcherType = "none"

[browser]
gatherUsageStats = false
