# Sample corpus (tracked in git)

This folder holds **small** example documents so a fresh clone can build an index immediately.

For your own content, prefer `data/corpus/` (gitignored) or uploads from the Streamlit UI.

See [../docs/document-ingestion.md](../docs/document-ingestion.md).
